services:
  - type: web
    name: agripred
    env: python
    plan: starter
    buildCommand: |
      echo "python-3.10.13" > runtime.txt
      pip install -r requirements.txt
    startCommand: python app.py
